import React from 'react'
import {connect} from 'react-redux'

const AudioContainer = connect(null, null)(
  props =>
    <div id="audio-container">
    Audio Recording Container Dummy
    </div>
)

export default AudioContainer
